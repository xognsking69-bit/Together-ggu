# Togetrip V3.28.1 Build Fix

- `package.json`과 `package-lock.json`을 동기화했습니다.
- 영수증 사용자별 금액 데이터의 `Expense.customShares` 타입을 복구했습니다.
- 현재 영수증 컴포넌트가 지원하지 않는 오래된 props 연결을 제거했습니다.
- 웹 CSS 및 CSS module 타입 선언을 추가했습니다.
- 표시 버전과 패키지 버전을 `V3.28.1`, iOS/Android 빌드 번호를 `17`로 맞췄습니다.
